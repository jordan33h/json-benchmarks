apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'shadow'
apply plugin: 'distribution'

buildDir = 'target'

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

ext.groovyVersion = System.properties.getProperty('groovyVersion', '2.3.5') // by default
println "Groovy version: ${groovyVersion}" 

dependencies {
    compile "org.codehaus.groovy:groovy-all:${groovyVersion}"
    compile 'com.fasterxml.jackson.core:jackson-databind:2.4.1.3'
    compile 'com.google.code.gson:gson:2.2.4'
    compile 'io.fastjson:boon:0.23'
    compile 'com.wizzardo.tools:tools:0.7'

    compile 'org.openjdk.jmh:jmh-core:0.9.5'
    compile 'org.openjdk.jmh:jmh-generator-annprocess:0.9.5'

//    compile fileTree(dir: 'libs', include: '*.jar')
}

repositories {
    jcenter()
}

buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.github.jengelman.gradle.plugins:shadow:0.8'
    }
}

shadow {
    outputFile = new File("${buildDir}/benchmarks.jar")

    transformer(com.github.jengelman.gradle.plugins.shadow.transformers.ManifestResourceTransformer) {
        mainClass = 'org.openjdk.jmh.Main'
    }
}

task createGradleWrapper(type: Wrapper) {
    gradleVersion = '1.11'
}
